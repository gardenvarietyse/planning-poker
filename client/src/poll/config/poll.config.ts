/*
  in a real application, we could separate test/prod configs
  with separate files
*/

export const POLL_CONFIG = {
  socketEndpoint: 'ws://localhost:3000',
  apiEndpoint: 'http://localhost:3000',
};
